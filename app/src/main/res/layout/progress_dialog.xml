<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/progress_indicator"
    android:background="@android:color/transparent">

    <ProgressBar
        android:id="@+id/pb_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <TextView
        android:id="@+id/tv_operation_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/please_wait"
        android:textStyle="bold"
        android:paddingTop="20dp"
        android:textColor="@color/black"
        app:layout_constraintTop_toBottomOf="@id/pb_indicator"
        app:layout_constraintStart_toStartOf="@id/pb_indicator"
        app:layout_constraintEnd_toEndOf="@id/pb_indicator"
        app:layout_constraintBottom_toBottomOf="@id/pb_indicator"/>


</androidx.constraintlayout.widget.ConstraintLayout>